version: "3.8"

services:
  frontend:
    image: afrazdev/cowlar:frontend-image1
    build:
      context: ./frontend
      dockerfile: dockerfile
    ports:
      - "5000:5000"
    container_name: cowlar_cont_frontend1

  backend:
    image: afrazdev/cowlar:backend-image1
    build:
      context: ./backend
      dockerfile: dockerfile
    ports:
      - "8000:8000"
    container_name: cowlar_cont_backend1
